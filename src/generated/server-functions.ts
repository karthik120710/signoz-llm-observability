// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  LLMUsageType,
  LLMUsageCreateInput,
  LLMUsageUpdateInput,
  BudgetRuleType,
  BudgetRuleCreateInput,
  BudgetRuleUpdateInput,
  BudgetAlertType,
  BudgetAlertCreateInput,
  BudgetAlertUpdateInput,
  SREIncidentType,
  SREIncidentCreateInput,
  SREIncidentUpdateInput,
  SigNozConfigType,
  SigNozConfigCreateInput,
  SigNozConfigUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// LLMUsage Client Functions
// ============================================================================

/**
 * List all LLMUsage records
 */
export async function getLLMUsageList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<LLMUsageType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/llmusages${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list LLMUsage')
  }
  const json = await response.json()
  return (json.items || []) as LLMUsageType[]
}

/**
 * Get a single LLMUsage by ID
 */
export async function getLLMUsageById(args: { data: { id: string; userId?: string } }): Promise<LLMUsageType> {
  const url = `${getApiBase()}/api/llmusages/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'LLMUsage not found')
  }
  const json = await response.json()
  return json.data as LLMUsageType
}

/**
 * Create a new LLMUsage
 */
export async function createLLMUsage(args: { data: { input: LLMUsageCreateInput; userId?: string } }): Promise<LLMUsageType> {
  const url = `${getApiBase()}/api/llmusages`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create LLMUsage')
  }
  const json = await response.json()
  return json.data as LLMUsageType
}

/**
 * Update an existing LLMUsage
 */
export async function updateLLMUsage(args: { data: { id: string; input: LLMUsageUpdateInput; userId?: string } }): Promise<LLMUsageType> {
  const url = `${getApiBase()}/api/llmusages/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update LLMUsage')
  }
  const json = await response.json()
  return json.data as LLMUsageType
}

/**
 * Delete a LLMUsage
 */
export async function deleteLLMUsage(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/llmusages/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete LLMUsage')
  }
  return { success: true }
}

// ============================================================================
// BudgetRule Client Functions
// ============================================================================

/**
 * List all BudgetRule records
 */
export async function getBudgetRuleList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<BudgetRuleType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/budget-rules${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list BudgetRule')
  }
  const json = await response.json()
  return (json.items || []) as BudgetRuleType[]
}

/**
 * Get a single BudgetRule by ID
 */
export async function getBudgetRuleById(args: { data: { id: string; userId?: string } }): Promise<BudgetRuleType> {
  const url = `${getApiBase()}/api/budget-rules/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'BudgetRule not found')
  }
  const json = await response.json()
  return json.data as BudgetRuleType
}

/**
 * Create a new BudgetRule
 */
export async function createBudgetRule(args: { data: { input: BudgetRuleCreateInput; userId?: string } }): Promise<BudgetRuleType> {
  const url = `${getApiBase()}/api/budget-rules`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create BudgetRule')
  }
  const json = await response.json()
  return json.data as BudgetRuleType
}

/**
 * Update an existing BudgetRule
 */
export async function updateBudgetRule(args: { data: { id: string; input: BudgetRuleUpdateInput; userId?: string } }): Promise<BudgetRuleType> {
  const url = `${getApiBase()}/api/budget-rules/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update BudgetRule')
  }
  const json = await response.json()
  return json.data as BudgetRuleType
}

/**
 * Delete a BudgetRule
 */
export async function deleteBudgetRule(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/budget-rules/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete BudgetRule')
  }
  return { success: true }
}

// ============================================================================
// BudgetAlert Client Functions
// ============================================================================

/**
 * List all BudgetAlert records
 */
export async function getBudgetAlertList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<BudgetAlertType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/budget-alerts${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list BudgetAlert')
  }
  const json = await response.json()
  return (json.items || []) as BudgetAlertType[]
}

/**
 * Get a single BudgetAlert by ID
 */
export async function getBudgetAlertById(args: { data: { id: string; userId?: string } }): Promise<BudgetAlertType> {
  const url = `${getApiBase()}/api/budget-alerts/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'BudgetAlert not found')
  }
  const json = await response.json()
  return json.data as BudgetAlertType
}

/**
 * Create a new BudgetAlert
 */
export async function createBudgetAlert(args: { data: { input: BudgetAlertCreateInput; userId?: string } }): Promise<BudgetAlertType> {
  const url = `${getApiBase()}/api/budget-alerts`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create BudgetAlert')
  }
  const json = await response.json()
  return json.data as BudgetAlertType
}

/**
 * Update an existing BudgetAlert
 */
export async function updateBudgetAlert(args: { data: { id: string; input: BudgetAlertUpdateInput; userId?: string } }): Promise<BudgetAlertType> {
  const url = `${getApiBase()}/api/budget-alerts/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update BudgetAlert')
  }
  const json = await response.json()
  return json.data as BudgetAlertType
}

/**
 * Delete a BudgetAlert
 */
export async function deleteBudgetAlert(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/budget-alerts/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete BudgetAlert')
  }
  return { success: true }
}

// ============================================================================
// SREIncident Client Functions
// ============================================================================

/**
 * List all SREIncident records
 */
export async function getSREIncidentList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<SREIncidentType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/sreincidents${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list SREIncident')
  }
  const json = await response.json()
  return (json.items || []) as SREIncidentType[]
}

/**
 * Get a single SREIncident by ID
 */
export async function getSREIncidentById(args: { data: { id: string; userId?: string } }): Promise<SREIncidentType> {
  const url = `${getApiBase()}/api/sreincidents/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'SREIncident not found')
  }
  const json = await response.json()
  return json.data as SREIncidentType
}

/**
 * Create a new SREIncident
 */
export async function createSREIncident(args: { data: { input: SREIncidentCreateInput; userId?: string } }): Promise<SREIncidentType> {
  const url = `${getApiBase()}/api/sreincidents`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create SREIncident')
  }
  const json = await response.json()
  return json.data as SREIncidentType
}

/**
 * Update an existing SREIncident
 */
export async function updateSREIncident(args: { data: { id: string; input: SREIncidentUpdateInput; userId?: string } }): Promise<SREIncidentType> {
  const url = `${getApiBase()}/api/sreincidents/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update SREIncident')
  }
  const json = await response.json()
  return json.data as SREIncidentType
}

/**
 * Delete a SREIncident
 */
export async function deleteSREIncident(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/sreincidents/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete SREIncident')
  }
  return { success: true }
}

// ============================================================================
// SigNozConfig Client Functions
// ============================================================================

/**
 * List all SigNozConfig records
 */
export async function getSigNozConfigList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<SigNozConfigType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/sig-noz-configs${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list SigNozConfig')
  }
  const json = await response.json()
  return (json.items || []) as SigNozConfigType[]
}

/**
 * Get a single SigNozConfig by ID
 */
export async function getSigNozConfigById(args: { data: { id: string; userId?: string } }): Promise<SigNozConfigType> {
  const url = `${getApiBase()}/api/sig-noz-configs/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'SigNozConfig not found')
  }
  const json = await response.json()
  return json.data as SigNozConfigType
}

/**
 * Create a new SigNozConfig
 */
export async function createSigNozConfig(args: { data: { input: SigNozConfigCreateInput; userId?: string } }): Promise<SigNozConfigType> {
  const url = `${getApiBase()}/api/sig-noz-configs`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create SigNozConfig')
  }
  const json = await response.json()
  return json.data as SigNozConfigType
}

/**
 * Update an existing SigNozConfig
 */
export async function updateSigNozConfig(args: { data: { id: string; input: SigNozConfigUpdateInput; userId?: string } }): Promise<SigNozConfigType> {
  const url = `${getApiBase()}/api/sig-noz-configs/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update SigNozConfig')
  }
  const json = await response.json()
  return json.data as SigNozConfigType
}

/**
 * Delete a SigNozConfig
 */
export async function deleteSigNozConfig(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/sig-noz-configs/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete SigNozConfig')
  }
  return { success: true }
}
